{
  "name": "updater-test",
  "private": true,
  "version": "0.0.3",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build": "npm run tauri -- build -v"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.2.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.2.0"
  }
}
